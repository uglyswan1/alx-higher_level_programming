#!/usr/bin/python3

def element_at(my_list, idx):
    """Retrieves an element from a list"""
    if idx < 0
        print("None")
    elif idx >= len(my_list)
        print ("None")
    else
        print (my_list["{:d}".format(my_list[idx])])
